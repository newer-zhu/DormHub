<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuhodor.server.mapper.MenuMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zhuhodor.server.model.pojo.Menu">
        <id column="id" property="id"/>
        <result column="url" property="url"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="name" property="name"/>
        <result column="iconCls" property="iconCls"/>
        <result column="keepAlive" property="keepAlive"/>
        <result column="requireAuth" property="requireAuth"/>
        <result column="parentId" property="parentId"/>
        <result column="enabled" property="enabled"/>
    </resultMap>
<!--    多级菜单-->
    <resultMap id="Menus" type="com.zhuhodor.server.model.pojo.Menu" extends="BaseResultMap">
        <collection property="children" ofType="com.zhuhodor.server.model.pojo.Menu">
            <id column="id2" property="id"/>
            <result column="url2" property="url"/>
            <result column="path2" property="path"/>
            <result column="component2" property="component"/>
            <result column="name2" property="name"/>
            <result column="iconCls2" property="iconCls"/>
            <result column="keepAlive2" property="keepAlive"/>
            <result column="requireAuth2" property="requireAuth"/>
            <result column="parentId2" property="parentId"/>
            <result column="enabled2" property="enabled"/>
        </collection>
    </resultMap>
<!--    菜单携带角色信息-->
    <resultMap id="MenusWithRoles" type="com.zhuhodor.server.model.pojo.Menu" extends="BaseResultMap">
        <collection property="roles" ofType="com.zhuhodor.server.model.pojo.Role">
            <id column="rid" property="id" />
            <result column="r_name" property="name" />
            <result column="role_name" property="roleName" />
        </collection>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, url, path, component, name, iconCls, keepAlive, requireAuth, parentId, enabled
    </sql>

    <!--获取菜单列表,并且携带允许访问的角色-->
    <select id="getMenusWithRole" resultMap="MenusWithRoles">
        select
            m.*,
            r.id rid, r.name r_name, r.role_name
        from p_menu m,
             p_menu_role mr,
             p_role r
        where
            m.id = mr.mid
            and mr.rid = r.id
        order by m.id;
    </select>

    <!--通过用户id获取菜单列表-->
    <select id="getMenusByUserId" resultMap="Menus">
        select
            distinct m1.*,
            m2.id as id2,
            m2.component as component2,
            m2.enabled as enables2,
            m2.iconCls as iconCls2,
            m2.keepAlive as keepAlive2,
            m2.name as name2,
            m2.parentId as parentId2,
            m2.requireAuth as requireAuth2,
             m2.url as url2,
            m2.path as path2
        from
            p_menu m1,
            p_menu m2,
            p_user_role ur,
            p_menu_role mr
        where
            m1.id = m2.parentId
            and ur.r_id = mr.rid
            and mr.mid = m2.id
            and m2.enabled = true
            and ur.u_id = #{userId}
        order by
            m1.id,
            m2.id
    </select>
</mapper>
